import React from 'react'
import { ITasks } from '../medels/ITasks'

interface tasksProps {
    taskprops: ITasks
}

function tasks(props: tasksProps) {
  return (
    <div>tasks</div>
  )
}

export default tasks